package com.cookandroid.gachon_study_room.adapter

import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView

class SeatAdapter : RecyclerView.Adapter<SeatAdapter.SeatHolder>(){
    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): SeatHolder {
    }

    override fun onBindViewHolder(holder: SeatHolder, position: Int) {
    }

    override fun getItemCount(): Int {
    }
    class SeatHolder(private val binding:) : RecyclerView.ViewHolder<>()
}